/** @type {import('next').NextConfig} */
const nextConfig = {
    // Next.js의 기본 설정
    reactStrictMode: true,
    // 이미지 도메인 설정 (필요한 경우 추가)
    images: {
        domains: [],
    }
}

module.exports = nextConfig
